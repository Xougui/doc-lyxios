---
title: L'équipe
description: Découvrez les membres de l'équipe de Lyxios.
sidebar:
  order: 0
template: splash
---


import { Card } from '@astrojs/starlight/components';

export const staff = await fetch('https://api-lyxios.xouxou-hosting.fr/api/v1/staff')
  .then((res) => res.json())
  .then((data) => (data.status === 'success' && Array.isArray(data.staff) ? data.staff : []))
  .catch((e) => {
    console.error('Erreur lors de la récupération du staff:', e);
    return [];
  });

export const customInfo = {
  '946098490654740580': {
    role: "Propriétaire, développeur, gérant (discord et bot), designeur, hébergeur",
    desc: "Passionné par le développement de bots Discord et l'optimisation des performances.",
    social: [
        { icon: "fab fa-discord", url: "https://discord.gg/RZHrtzwUC2", title: "Discord" },
        { icon: "fab fa-github", url: "https://github.com/Xougui", title: "GitHub" }
    ]
  },
  '1178647820052467823': {
    role: "Propriétaire, développeur",
    desc: "Développeur passionné, il gère entièrement Alt F4, son bot.",
    social: [
        { icon: "fab fa-github", url: "https://github.com/Kadawatcha", title: "GitHub" },
        { icon: "fab fa-discord", url: "https://discord.gg/2B53yNdWPG", title: "Discord" }
    ]
  },
  '1223319413760200804': {
    role: "Gérant Blacklist",
    desc: "Chargé de la gestion de la Blacklist, il œuvre pour un Discord plus sain.",
    social: [
        { icon: "fab fa-youtube", url: "https://www.youtube.com/@Dogday_nap", title: "YouTube" }
    ]
  },
  '1242861510099992690': {
    role: "Gérant Blacklist",
    desc: "Il gère la Blacklist.",
    social: [
        { icon: "fab fa-discord", url: "https://discord.gg/HM2gE5f52b", title: "Discord" },
        { icon: "fab fa-github", url: "https://github.com/PhantomX-Bs", title: "GitHub" }
    ]
  },
  '1402631368806109255': {
    role: "Community Manager",
    desc: "Il se consacre à accroître la visibilité de Lyxios.",
    social: []
  }
};

{/* Chargement de FontAwesome pour les icônes */}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

<div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
  {staff.map((member) => {
    const info = customInfo[member.id] || {};
    return (
      <Card title={member.display_name || member.name}>
        <div style={{ display: 'flex', flexWrap: 'wrap', alignItems: 'center', gap: '1.5rem' }}>
          <div style={{ position: 'relative', display: 'inline-block', flexShrink: 0 }}>
            <img 
              src={member.avatar_url} 
              alt={member.display_name} 
              style={{ 
                borderRadius: '50%', 
                width: '80px', 
                height: '80px', 
                objectFit: 'cover',
                border: '3px solid var(--color-lyxios-green-500)',
                boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.3)'
              }} 
            />
            {member.avatar_decoration_url && (
              <img 
                src={member.avatar_decoration_url} 
                alt="Decoration"
                style={{ position: 'absolute', top: '30%', left: '50%', transform: 'translate(-50%, -50%)', width: '120%', height: 'auto', maxWidth: 'none', zIndex: 20, pointerEvents: 'none' }} 
              />
            )}
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
            {info.role && (
              <div style={{ fontWeight: 'bold', color: 'var(--sl-color-text-accent)' }}>
                {info.role}
              </div>
            )}
            {info.desc && (
              <div style={{ fontSize: '0.9em', opacity: 0.9 }}>
                {info.desc}
              </div>
            )}
            {info.social && info.social.length > 0 && (
              <div style={{ display: 'flex', gap: '0.8rem', marginTop: '0.2rem' }}>
                {info.social.map((s, i) => (
                  <a key={i} href={s.url} target="_blank" rel="noopener noreferrer" title={s.title} style={{ color: 'var(--sl-color-text-accent)', fontSize: '1.2em', textDecoration: 'none' }}>
                    <i className={s.icon}></i>
                  </a>
                ))}
              </div>
            )}
          </div>
        </div>
      </Card>
    );
  })}
</div>